{
  "name": "bot-conversation",
  "version": "1.0.0",
  "description": "Zero-dependency library for chat-bot creators with deadlines.",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"build": "tsc",
	"distribute": "cp ./package.json ./dist/package.json",
	"prepublish": "npm run build && npm run distribute",
	"publish": "npm publish ./dist"
  },
  "keywords": [
    "chat-bot",
    "conversation",
    "algorithm",
    "restore"
  ],
  "author": "Roman Melamud",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.21",
    "typescript": "^4.5.5"
  }
}
